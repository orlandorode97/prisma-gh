generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DSN")
}


model user {
  id    Int    @id @default(autoincrement())
  name String @unique
  email String?
}

model Post {
  id    Int    @id
  title String
}

